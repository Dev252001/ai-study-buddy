# CD Pipeline — disabled until a production server is configured.
# To enable deployment, add SSH_HOST / SSH_USER / SSH_KEY to GitHub Secrets
# and uncomment the jobs below.
#
# on:
#   push:
#     branches: [main]
